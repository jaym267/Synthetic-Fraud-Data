# Milestone 1 — EDA + environment
# Pinned to the versions verified working on Python 3.12.10 / Windows.
# Regenerate a full lockfile with:  pip freeze > requirements.lock.txt

# Core data / EDA
pandas==2.3.3
numpy==2.5.1
scikit-learn==1.9.0
scipy==1.18.0
matplotlib==3.11.0
seaborn==0.13.2

# Synthetic data generator (used from Milestone 2 onward; pulls torch CPU build)
ctgan==0.12.1

# Dataset download
kaggle==2.2.3
